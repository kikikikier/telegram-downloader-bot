# Deploy 2026-05-30

Deployed to:

```text
95.181.213.67:/home/codex/telegram-downloader-bot
```

Backup created on server:

```text
/home/codex/telegram-downloader-bot/backups/20260529-223300
```

Server status after restart:

- Bot process: running, PID `374476`.
- Proxy: running on `127.0.0.1:18080`.
- `ffmpeg` and `ffprobe`: present.
- `yt-dlp`: `2026.03.17`.
- `telethon`: `1.43.2`.
- Telegram via proxy: OK.
- Disk: about 92 GB free.

Current large-file mode:

- `BOT_MAX_MB=49`.
- `BOT_PART_MB=48`.
- `BOT_DIRECT_PROXY` set.
- `TELEGRAM_API_ID` set on server.
- `TELEGRAM_API_HASH` set on server.
- MTProto single-file large uploads are active for new large downloads after the latest restart.
- Latest observed bot PID after cleanup/UX deployment: `382943`.
- Temporary folders are kept for `BOT_CLEANUP_DELAY_SECONDS=60` seconds after a job ends, then deleted by the cleanup worker.
- Cleanup worker test passed: a stale dummy folder under `/tmp/telegram-downloader-bot` was removed automatically.

Live logs:

```bash
cd /home/codex/telegram-downloader-bot
./logs.sh
```
